spring:
  datasource:
    url: jdbc:h2:mem:demoDb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: demouser
    password: demopass
    driver-class-name: org.h2.Driver
    initialization-mode: always

server:
  servlet:
    context-path: /api
